# Production environment defaults (committed - no secrets here).
# Only non-destructive @smoke tests should run against production (see `npm run test:prod`).
BASE_URL=https://www.demoblaze.com
API_BASE_URL=https://api.demoblaze.com
HEADLESS=true
LOCAL_RETRIES=1
CI_RETRIES=1
LOG_LEVEL=warn
