# Development environment defaults (committed - no secrets here).
# Secrets such as TEST_PASSWORD come from `.env` locally or the CI credential store.
BASE_URL=https://www.demoblaze.com
API_BASE_URL=https://api.demoblaze.com
HEADLESS=true
LOCAL_RETRIES=0
CI_RETRIES=2
LOG_LEVEL=info
